# v1.9.0 实施验收结果

状态：功能实现工作树验收通过，尚未形成独立 v1.9 Commit/Tag。

## 实施基线与环境

- 分支：`upgrade/v1.7-paper-code-alignment`
- v1.8 基线 Commit：`db6685a45baa5f75e4856cbc406e410ad313f332`
- 基线 Tag：`v1.8.0`
- 项目 Python：`/Users/qiu_star/miniforge3/envs/code-research-agent/bin/python`，Python 3.11.15
- Node：v24.15.0
- 当前 v1.9 为未提交工作树；不得把本文件当作正式 v1.9 Git 基线。

## 已实现能力

- 严格 Evaluation Subject、Dataset/Version/Case、六类 Input/Gold/Outcome、Run/CaseResult、Metric、
  Comparison/Gate、Bad Case/Occurrence/Event/Verification、Promotion、Replay 与 Artifact Schema。
- Frozen Dataset、双人 Human Gold门禁、Reference/Candidate Index隔离、Artifact Resolver/Hash校验。
- 六组件 deterministic Adapter、版本化 Metric Engine、Environment/Fingerprint/共同 Case Comparison。
- EvaluationRun与BaselineBinding分离；CI/Release/Manual Gate Rule合同。
- Bad Case fingerprint去重、append-only occurrence/event、FixReference和case-level verification。
- Offline Replay readiness、Live Trial/Repeat、显式consent/budget和Provider并发上限。
- 独立`data/evaluation.sqlite3`、编号migration、WAL、Lease、Cancel/Retry、Idempotency和短事务。
- Evaluation Coordinator、独立Evaluation Trace、受控API、local-admin默认策略和最小Dashboard。
- 无网络CI命令、Alignment Gold审计命令和deterministic性能脚本。

## 自动验收

`bash scripts/validate.sh`真实结果：

- 后端：413 passed，6 warnings，53.80s。
- 前端：18 files / 31 tests passed。
- 前端typecheck与production build通过。
- Build contract通过：Mermaid保持动态chunk，TypeScript无生成物污染。
- 自动测试未调用真实Provider、未发送OTLP、未下载模型。

Evaluation专项：40 passed。50次deterministic suite性能结果：mean 1.972ms，P95 1.916ms，
network requests=0。该数字只表示本机synthetic合同Runner开销，不是业务模型端到端性能。

## Alignment Gold技术债

`scripts/validate_alignment_gold.py --release`真实输出为：0 Dev pair、0 Locked pair、0 positive、
0 negative，状态`ALIGNMENT_BENCHMARK_PENDING`。因此：

- v1.9 synthetic Alignment case只验证Schema/Adapter/Metric合同。
- 不声明Alignment Accuracy、F1、Calibration或Release质量完成。
- 正式Release Gate仍需4 Dev + 2 Locked、72正例 + 20 hard negative、双人标注和adjudication。

## 已知限制

- 当前没有统一认证系统，Evaluation API默认关闭；启用后第一版仅本机管理员。
- Live Experiment合同、repeat隔离和预算门禁已实现，但自动验收不执行真实Provider。
- LLM Judge不是主要指标，当前实现未用LLM自动生成Gold或自动关闭Bad Case。
- 当前结果尚未提交；正式Baseline需新的完整Commit SHA，worktree patch不能晋升正式Baseline。
