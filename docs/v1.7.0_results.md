# v1.7.0 实施与验收结果

验收日期：2026-07-18

## 已实现

- 显式 Profile type/granularity、稳定 merge/split ID 和正文/Figure/Formula 聚合。
- 八路 Candidate 合并、repo/index 隔离和 Legacy Baseline 保留。
- 九类 Feature、missing/not-applicable/required-missing、权重 coverage penalty。
- Candidate-level Calibration、逐 Selection relation、集合决策、abstain/no-implementation 门禁。
- 只允许 Top-K Candidate/Evidence 的结构化 Provider Verifier 和 Scorer fallback。
- 独立 Alignment SQLite、阶段 staging、attempt/retry、Lease、Coordinator、Cancel 和显式 Deployment。
- Append-only Review、effective revision、逐 Candidate Evidence 校验。
- 始终注册且默认关闭的 Alignment API，以及带 authority/evidence role 的 v1.6 `get_alignment` 接入。
- Dev/Locked 分离的 Benchmark/Prediction Schema、核心指标和离线评测脚本。

## 基线与兼容

v1.7 基于 commit `c6c3337f12935cb5251a966e8ca673366f5e8cc2`（tag `v1.6.0`）开发。
Legacy `align_paper_to_code()`、旧 `paper_code_alignment.json`/报告/Edge、v1.4 事实 Schema、v1.5
Retrieval 排序、v1.6 Agent Graph 和前端均未被替换。

## 自动验收

```text
python -m pytest -q
343 passed, 6 warnings in 53.62s

npm --prefix frontend test
16 files passed, 29 tests passed

npm --prefix frontend run build
passed（含既有 Mermaid chunk size warning）

bash scripts/validate.sh
passed / Validation completed
```

Alignment 专项 31 passed；Alignment + Research Agent + Prompt 安全合同 61 passed。自动测试使用
Fake/Mock，不下载模型、不访问真实 Provider。警告为既有 FastAPI TestClient/httpx2、PyMuPDF
SWIG 类型弃用提示和前端 Mermaid 大 chunk 提示，本轮未识别为 v1.7 功能失败。

## 尚未满足的质量门禁

仓库当前没有 6 对获得授权、固定版本且完成双人标注的 repo-paper fixtures，也没有 72 个正例和
20 个 hard negative Gold。因此不能真实报告 Candidate Recall、Final F1、Abstention、Brier/ECE、
Locked Test 延迟或 8 组消融结论。代码明确保留此门禁，未用 LLM 或当前对齐器伪造 Gold。
